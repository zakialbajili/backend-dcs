/*
  Warnings:

  - You are about to drop the column `file` on the `history_work_order` table. All the data in the column will be lost.

*/
-- AlterTable
ALTER TABLE `history_work_order` DROP COLUMN `file`;
